{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "User profile with E2EE keys",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "username": { "type": "string" },
        "displayName": { "type": "string" },
        "bio": { "type": "string" },
        "messageExpiryHours": { "type": "number" },
        "messageExpiryActivatedAt": { "type": "number" },
        "onboardingCompleted": { "type": "boolean" },
        "notificationsEnabled": { "type": "boolean" },
        "fcmToken": { "type": "string" },
        "fcmTokens": { "type": "array" },
        "publicKey": { "type": "string" },
        "encryptedPrivateKey": { "type": "string" },
        "salt": { "type": "string" },
        "createdAt": { "type": "number" },
        "updatedAt": { "type": "number" }
      },
      "required": ["uid", "username", "publicKey", "createdAt"]
    },
    "Message": {
      "title": "Message",
      "description": "An encrypted anonymous message",
      "type": "object",
      "properties": {
        "receiverId": { "type": "string" },
        "encryptedContent": { "type": "string" },
        "createdAt": { "type": "number" },
        "read": { "type": "boolean" },
        "isFlagged": { "type": "boolean" },
        "reaction": { "type": "string" },
        "archived": { "type": "boolean" },
        "archivedAt": { "type": "number" },
        "tags": { "type": "array" }
      },
      "required": ["receiverId", "encryptedContent", "createdAt", "read", "isFlagged"]
    },
    "Report": {
      "title": "Report",
      "description": "A user report for inappropriate messages or users",
      "type": "object",
      "properties": {
        "messageId": { "type": "string" },
        "reporterId": { "type": "string" },
        "reason": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["messageId", "reporterId", "reason", "createdAt"]
    },
    "PasswordReset": {
      "title": "PasswordReset",
      "description": "6-digit OTP password reset verification request",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "codeHash": { "type": "string" },
        "expiresAt": { "type": "number" },
        "attempts": { "type": "number" },
        "verified": { "type": "boolean" },
        "createdAt": { "type": "number" }
      },
      "required": ["email", "codeHash", "expiresAt", "attempts", "verified", "createdAt"]
    }
  },
  "firestore": {
    "users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "Stores user profiles with their public key."
    },
    "users/{userId}/messages/{messageId}": {
      "schema": { "$ref": "#/entities/Message" },
      "description": "Anonymous messages received by the user."
    },
    "reports/{reportId}": {
      "schema": { "$ref": "#/entities/Report" },
      "description": "Reports of inappropriate messages."
    },
    "passwordResets/{resetId}": {
      "schema": { "$ref": "#/entities/PasswordReset" },
      "description": "Temporary password reset requests with hashed verification codes."
    }
  }
}
